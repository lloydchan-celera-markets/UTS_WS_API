package com.vectails.data;

import java.util.ArrayList;
import java.util.List;

import org.w3c.dom.Element;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

public class QuoteCreator implements IXmlParser{
	
	private String Code = null; 
	
	public void setCode(String code) {
		Code = code;
	}

	List<SubscribedUnderlyings> myList = new ArrayList<SubscribedUnderlyings>();
	
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("QuoteCreator [ Code=" + Code + ", ");
		for (SubscribedUnderlyings l: myList) {
			sb.append(l.toString());
		}
		sb.append("]");
		return sb.toString();
	}
	
	public void parseXmlElement(Element root) {

		this.parseAttribute(root);

		NodeList nl = root.getChildNodes();
		int lenNl = nl.getLength();
		for (int i = 0; i < lenNl; i++) {
			Node n1 = nl.item(i);
			if (n1.getNodeType() == Node.ELEMENT_NODE
					&& IXmlTag.SubscribedUnderlyings.toString().equals(n1.getNodeName())) {
				myList.parseXmlElement((Element) n1);
			}
		}
		System.out.println(this.toString());
	}
}
