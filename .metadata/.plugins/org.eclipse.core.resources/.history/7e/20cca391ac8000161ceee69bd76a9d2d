package com.vectails.data;

import java.util.ArrayList;
import java.util.List;

import org.apache.cxf.common.i18n.Exception;
import org.w3c.dom.Element;
import org.w3c.dom.NamedNodeMap;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

public class SubscribedUnderlyings implements IXmlParser{
	List<UnderlyingType> myList = new ArrayList<UnderlyingType>();
	
	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("QuoteCreator [ ");
		for (SubscribedUnderlyings l: myList) {
			sb.append(l.toString());
		}
		sb.append("]");
		return sb.toString();
	}
	
	public void parseXmlElement(Element root) {

		this.parseAttribute(root);

		NodeList nl = root.getChildNodes();
		int lenNl = nl.getLength();
		for (int i = 0; i < lenNl; i++) {
			Node n1 = nl.item(i);
			if (n1.getNodeType() == Node.ELEMENT_NODE
					&& IXmlTag.UnderlyingType.toString().equals(n1.getNodeName())) {
				myList.parseAttribute((Element)n1);
				myList.parseXmlElement((Element) n1);
			}
		}
		System.out.println(this.toString());
	}
}
