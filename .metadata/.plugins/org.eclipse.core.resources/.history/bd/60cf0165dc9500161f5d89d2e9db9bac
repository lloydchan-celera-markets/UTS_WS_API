package com.celera.message.cmmf;

interface ICharEnum<T extends Enum>
{
	final char asChar = ' ';
	
    public T static T getEApp(final char asChar)
    {
        for (T v : T.values())
            if (v.asChar == asChar)
                return v;
        return null;
    }
}
