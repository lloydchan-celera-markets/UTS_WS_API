package com.vectails.data;

import java.util.ArrayList;
import java.util.List;

import org.w3c.dom.Element;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

public class Underlyings {
	
	private List<Underlying> myList = new ArrayList<Underlying>();
	
	public void parseXmlElement(Element el) {
//      List<Currency> l = new ArrayList<Currency>();
      
		NodeList nl = el.getChildNodes();
		int len = nl.getLength();
		for (int i =0 ; i<len; i++) {
			Node n = nl.item(i);
			if (n.getNodeType() == Node.ELEMENT_NODE) {
				Underlying o = new Underlying();
				o.parseAttribute((Element)n);
	
		        System.out.println(o);
		        
		        myList.add(o);
			}
		}
		
//      return l;
	}
}
